# Express Store API Prototype

Use this folder to validate Express request/response extraction.

Expected route examples:

- `GET /products`
- `GET /products/{productId}`
- `POST /products`
- `PATCH /products/{productId}`
- `GET /health`
