# FastAPI Blog API Prototype

Use this project as the required PS08 working prototype input.

Local folder path to paste in the UI:

```text
C:\Users\NAVEENYA\OneDrive\Desktop\NAVEENYA\api_doc_generator\prototype_projects\fastapi_blog_api
```

Expected extraction coverage:

- `GET /health`
- `GET /api/v1/articles/`
- `GET /api/v1/articles/{article_id}`
- `POST /api/v1/articles/`
- `DELETE /api/v1/articles/{article_id}`
- Query params, headers, request body schema, response models, and 4xx error responses.
